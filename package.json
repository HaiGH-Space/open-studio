{
  "name": "open-studio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "catalog:generate": "pnpm --filter web run catalog:generate",
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "typecheck": "pnpm --filter web typecheck",
    "lint": "pnpm --filter web lint",
    "test": "pnpm --filter web test"
  }
}
